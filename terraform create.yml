---
- name: Create VM on RHV using Terraform
  hosts: localhost
  connection: local
  gather_facts: false
  
  tasks:
    - name: Create and run VM from template using Terraform
      terraform:
        project_path: "~/terraform/ovirt/"
        state: "{{ terraform_state }}"